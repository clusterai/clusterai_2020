from typing import List

def download(source: str='clusterAI',destination: str='local'):
    """
    English
    source: str that flags from where the dataset should be downloaded.
    destination: str that flags where the dataset should be left, preps google collab if necessary. 
    """
    print(source)
    print(destination)